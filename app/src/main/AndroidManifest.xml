<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.glassy.salesmanager">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".Client.ClientActivity"
            android:parentActivityName=".MainActivity">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".MainActivity"></meta-data>
        </activity>
        <activity
            android:name=".Client.AddClientActivity"
            android:parentActivityName=".Client.ClientActivity">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".Client.ClientActivity"></meta-data>
        </activity>
        <activity android:name=".Client.ReadClientActivity" />
        <activity android:name=".Product.ProductActivity" />
        <activity android:name=".Sale.SaleActivity" />
        <activity android:name=".Product.AddProductActivity" />
        <activity android:name=".Sale.AddSaleActivity" />
        <activity android:name=".Product.ReadProductActivity" />
        <activity android:name=".Stats.StatsActivity" />
        <activity android:name=".Client.EditClientActivity"></activity>
    </application>

</manifest>